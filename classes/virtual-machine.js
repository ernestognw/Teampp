const { OPCODES } = require("./utils");

const {
  SUM,
  SUB,
  MULT,
  DIV,
  EQ,
  NEQ,
  GTE,
  LTE,
  GT,
  LT,
  AND,
  OR,
  NOT,
  EQUAL,
  READ,
  WRITE,
  GOTO,
  GOTOF,
} = OPCODES;

class VirtualMachine {
  constructor() {}
}

module.exports = VirtualMachine;
