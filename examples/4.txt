program teamPP; 

class Person {
  vars age : integer;
  vars name[30] : char;

  int function example(x : int, y : float);
    vars another[100] : char;
    vars example, example2 : string; 
  {
    return(id);
  }
}

vars one, two, three : int;
vars four, five : float;
vars six : char;
vars seven, eight, nine, ten : string;

main(){
	leer.el();
	otro();
}
